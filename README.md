## Setup Instructions

1. Open `vban_router.py` in your favorite text editor.
2. Locate the `ROUTES` dictionary at the top of the file.
3. Replace the placeholder IPs (`10.0.0.x`) with the actual static IPs of the computers on your network.
4. Run the script: `python3 vban_router.py`